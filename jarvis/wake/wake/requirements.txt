# JARVIS Production Wake Word System
# =====================================
# Core (required)
openwakeword>=0.6.0
faster-whisper>=0.10.0
sounddevice>=0.4.6
numpy>=1.24.0
onnxruntime>=1.16.0

# Voice Activity Detection (strongly recommended)
# webrtcvad has C extension — use wheels package for easier install
webrtcvad-wheels>=2.0.11

# Noise suppression (strongly recommended)
noisereduce>=3.0.0

# Speaker verification (optional — enables owner-only mode)
resemblyzer>=0.1.1

# Audio resampling (optional — improves quality vs numpy fallback)
scipy>=1.10.0

# Dataset tools
soundfile>=0.12.1

# Fallback training (optional — only needed if OWW training API unavailable)
scikit-learn>=1.3.0

# Already in JARVIS requirements:
# flask, requests, python-dotenv, openai, psutil
